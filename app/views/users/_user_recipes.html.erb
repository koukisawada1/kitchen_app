<!-- ユーザのレシピ一覧 -->
<table class="table">
    <tbody>
        <% recipes.each do |recipe| %>
            <tr>
                <td>
                    <%= link_to recipe_path(recipe.id) do %>
        	        	<%= attachment_image_tag(recipe, :image, fallback: "no_image2.jpg", size: '130x130') %>
                    <% end %>
                </td>
                <td>
                    <h4><%= recipe.title %></h4>
                    <h5><%= simple_format(recipe.body) %></h5>
                    <h5 class="recipe-user">分量：<%= recipe.quantity %>人前  費用：<%= recipe.price %>円前後</h5>
                </td>
                <td class="recipe-user">
                    <div class="row">
                        <div class="col-md-3">
                            <%= link_to user_path(recipe.user) do %>
                                <%= attachment_image_tag(recipe.user, :image, fallback: "no_image.jpg", size: '70x70', class: "img-circle") %>
                            <% end %>
                        </div>
                        <div class="col-md-9">
                            <h5><%= recipe.user.name %>さん <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></h5>
                            <h5>
                                <span class="glyphicon glyphicon-heart" style="color: tomato;"><%= recipe.likes.count %></span>&nbsp;
                                <span class="glyphicon glyphicon-comment"><%= recipe.comments.count %></span>
                            </h5>
                        </div>
                    </div>
                </td>
            </tr>
        <% end %>
    </tbody>
</table>